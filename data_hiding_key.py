import numpy as np

def generate_data_hiding_key(height, width):
    # Replace this with your implementation to generate the data hiding key
    data_hiding_key = np.random.randint(2, size=(height, width))
    return data_hiding_key
